# Transformers stable release with official Qwen3.5 implementation. GPU smoke
# records installed versions; pin the resolved environment for each trajectory.
torch>=2.10,<3
transformers>=5.2,<6
accelerate>=1.12,<2
bitsandbytes>=0.49,<1
safetensors>=0.7,<1
huggingface_hub>=1.4,<2
sentencepiece>=0.2
pytest>=8
# Optional speedups (install separately against the pod's Torch/CUDA):
# flash-linear-attention; causal-conv1d
